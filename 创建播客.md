# 创建播客

先掌握 episode_profile，再学 briefing / briefing_suffix，然后看 speakers_config.json、episodes_config.json，最后再改 prompt 模板和 provider/model 配置。

```mermaid
flowchart TD
    A["LangGraph 调用 generate_outline_node(state, config)"] --> B["读取 config['configurable']"]
    B --> C["解析 outline_provider / outline_model / outline_config"]
    C --> D["合并默认模型配置"]

    D --> D1["默认 max_tokens = 3000"]
    D --> D2["默认 structured = JSON"]
    D --> D3["outline_config 覆盖默认值"]

    D --> E["AIFactory.create_language(...)"]
    E --> F["to_langchain() 转成 LangChain 异步模型"]

    B --> G["get_retry_config(configurable)"]
    G --> H["create_retry_decorator(...)"]
    H --> I["定义带 retry 的 _invoke_and_parse(prompt_text)"]

    I --> I1["outline_model.ainvoke(prompt_text) 调用 LLM"]
    I1 --> I2["extract_text_content(result.content)"]
    I2 --> I3["统一不同 provider 的响应格式为文本"]
    I3 --> I4["clean_thinking_content(content)"]
    I4 --> I5["移除 <think>...</think> 思考块"]
    I5 --> I6["outline_parser.invoke(content)"]
    I6 --> I7["解析并校验为 Outline Pydantic 对象"]

    A --> J["get_outline_prompter()"]
    J --> K["通过配置管理器加载 outline 模板"]

    K --> K1["优先使用 inline templates"]
    K --> K2["其次使用配置的 prompts_dir"]
    K --> K3["其次使用当前目录 prompts"]
    K --> K4["最后使用包内置模板"]

    A --> L["从 state 取渲染数据"]
    L --> L1["briefing：播客方向"]
    L --> L2["num_segments：段落数量"]
    L --> L3["content：原始素材"]
    L --> L4["speaker_profile.speakers：说话人背景"]
    L --> L5["language：目标语言"]

    K --> M["outline_prompt.render(...)"]
    L --> M

    M --> N["生成 outline_prompt_text"]
    N --> O["调用 _invoke_and_parse(outline_prompt_text)"]

    O --> P{"调用或解析是否出现可重试错误？"}
    P -- "是，例如临时 API 故障 / 429" --> O
    P -- "否，成功" --> Q["得到 outline_result"]
    P -- "否，不可重试错误" --> X["抛出异常，节点失败"]

    Q --> R["记录生成了多少 segments"]
    R --> S["return {'outline': outline_result}"]
    S --> T["LangGraph 合并到 PodcastState"]
    T --> U["下一个节点 generate_transcript_node 使用 outline"]

```

这个项目的核心就是：

`用户文档 / 文本内容
    ↓
LLM 生成 outline 大纲
    ↓
LLM 根据 outline 逐段生成 script / transcript 对话稿
    ↓
按 Dialogue 分片调用 TTS
    ↓
生成多个 mp3 clips
    ↓
合并 clips
    ↓
得到最终 podcast 音频`

对应到代码就是这条 LangGraph workflow：

E:\py\podcast-creator\src\podcast_creator\graph.py:22

`START
  → generate_outline
  → generate_transcript
  → generate_all_audio
  → combine_audio
  → END`

[podcast_generation_flow](https://www.notion.so/podcast_generation_flow-34e711589ea5806fa39ff736680438cc?pvs=21)

[podcast_generation_pipeline](https://www.notion.so/podcast_generation_pipeline-34e711589ea580a3982fdba953433c13?pvs=21)